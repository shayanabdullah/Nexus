{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "Represents fully authenticated system operator profile",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "The unique user identifier matching request.auth.uid" },
        "fullName": { "type": "string", "description": "The custom operator full identity name" },
        "email": { "type": "string", "description": "Routing core email address of user" },
        "operatorId": { "type": "string", "description": "Unique operator serial code" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "fullName", "email", "operatorId", "createdAt", "updatedAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Fully isolated secure operator profile collection"
    }
  }
}
